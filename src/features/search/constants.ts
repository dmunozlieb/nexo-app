/** Breakpoints de la pantalla de exploracion (anchos en px). */
export const BREAKPOINTS = {
  /** >= tablet: layouts mas anchos (grid de 2 columnas, hero horizontal). */
  tablet: 720,
  /** >= desktop: panel lateral sticky y flechas de carrusel. */
  desktop: 1180,
} as const;
